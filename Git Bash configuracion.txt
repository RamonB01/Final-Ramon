Git Bash configuracion

Mirar la configuracioon de GIT
 git config --list

Configurar usuario (nivel global)
git config --global user.name "Ramon Barrera"
git config --global user.email rebcontacto@gmail.com

Hay que crear una carpeta abrir el cmd de sa carpeta 

Crear con comando
echo texto del html > "nombre del archivo.html"

Crear el repositorio
git init

Ver el estado actual del repositorio
(algun cambio no guardado, si ya se guardó, un archivo no se agregó, etc)
git status

Agregar archivos al repositorio
git add . (agrega todos los archivos)
git add "nombre_del_archivo.tipo" (agrega el archivos especificado)

Agregar commit (mensaje) sobre los cambios
git commit -m "Mensaje sobre cambio y/o commit"

Ver historial de commit´s
git log

Ver ramas
git branch

Cambiar de Ramas y/o Volver a commit
git checkout (codigo de commit)
git checkout 

Cambiar de rama
git switch master (en este caso la rama es master)

Crear nueva rama y poner los cambios en ella
git switch -c "nombre_de_rama"

Usar git con github
Crear el repositorio en github (ya logeados)
copiar el https
git remote add origin https://github.com/nombre-usuario/nombre-repo.git
git push origin master (subimos la rama master a github)

Subir cambios en github desde git
git status
git add .
git commit -m "Escribir cambios"
git push 
git status

Clonar:
git clone https://github.com/usuario/nombre-repo.git
Entrar a la carpeta:
cd nombre-repo


Actualizar repo de GitHub
git pull origin main


1️⃣ Concepto básico

git merge se usa para combinar cambios de una rama en otra.

Por ejemplo: tenés una rama main y otra feature (nueva funcionalidad).

Querés que los cambios de feature pasen a main.

2️⃣ Flujo típico de trabajo
a) Estar en la rama destino
git checkout main


Esto indica que querés combinar cambios en main.

b) Hacer merge de la otra rama
git merge feature


feature es la rama origen que querés unir a la rama actual (main).

Git intentará fusionar los cambios automáticamente.

3️⃣ Posibles resultados
a) Merge automático exitoso

Git combina los cambios y genera un commit de merge.

Ejemplo de salida:

Updating abc1234..def5678
Fast-forward
 file.txt | 2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)

b) Conflictos

Si dos ramas modificaron la misma línea, Git no puede decidir y deja un conflicto.

Se verá así en los archivos:

<<<<<<< HEAD
código en main
=======
código en feature
>>>>>>> feature


Debés elegir qué versión conservar o combinar ambas.

Luego:

git add archivo_conflicto
git commit

4️⃣ Tips importantes

Siempre hacer git pull antes de merge si trabajás con repositorio remoto.

Usar --no-ff si querés forzar un commit de merge incluso cuando podría hacerse fast-forward:

git merge --no-ff feature


Ver ramas disponibles:

git branch

🔹 Ejemplo completo
git checkout main       # paso 1: voy a la rama destino
git pull                # paso 2: actualizo main
git merge feature       # paso 3: combino la rama feature en main


Si hay conflictos → resolverlos, luego:

git add .
git commit